# Environment / secrets — never commit these to a static-site repo
.env
.env.*
*.pem
*.key
secrets/
credentials*

# Dependencies (if a build step is ever added)
node_modules/

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# OS junk
.DS_Store
Thumbs.db

# Editor / IDE
.vscode/
.idea/
*.swp
*.swo

# Build / cache (reserved for future use)
dist/
build/
.cache/
.parcel-cache/
